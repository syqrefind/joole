import React from 'react';
import styles from './NotFound.css';

const NotFound = (props) => {
    return (
        <h1 className={styles.error404}>404 Not Found</h1>
    );
};

export default NotFound;